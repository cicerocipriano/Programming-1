#include <iostream>

using namespace std;

int main (int argc, char **argv) {
  
  int d;
  int a, l, p;

  cin >> d;
  cin >> A >> L >> P;

  if(A >= N && L >= N && P >= N)
    cout << "S" << endl;
  else
    cout << "N" << endl;

  return 0;
}	 
